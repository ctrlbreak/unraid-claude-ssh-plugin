# claude-ssh plugin defaults.
# The runtime allowlist (plugins + containers) lives separately at
# /boot/config/plugins/claude-ssh/allowlist.cfg and is edited through the
# Settings UI's Allowlist card. Future user-tunable settings (filter version
# pin, audit log retention, etc.) would be added here.
